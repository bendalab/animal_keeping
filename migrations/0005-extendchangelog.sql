-- -----------------------------------------------------
-- Table `animal_keeping`.`changelog`
-- -----------------------------------------------------

ALTER TABLE `animal_keeping`.`changelog` ADD `who` VARCHAR(200) NOT NULL;

